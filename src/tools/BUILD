# LFS Build Tools Package
# This package contains Starlark rules for building LFS packages
# The .bzl rules here are consumed via load(), which does not require
# exports_files; this file exists only to mark //tools as a package.
